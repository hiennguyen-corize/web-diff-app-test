import { ProjectListPage } from '@/components/pages/ProjectListPage';

export const metadata = {
  title: 'Web Diff - Projects',
};

export default function ProjectsPage() {
  return <ProjectListPage />;
}
