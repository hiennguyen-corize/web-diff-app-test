import { Config } from '@/types/config';
const config: Config = {
  client: {
    origin: 'https://diff-t.web.app',
  },
  cloudFunctions: {
    origin: 'https://asia-southeast1-diff-t.cloudfunctions.net',
  },
};

export default config;
