import { ButtonWrapper } from '@/components/ui/Button/styles';
import styled from 'styled-components';

export const ScreenshotSlotsShowWrapper = styled.div`
  ${ButtonWrapper} {
    padding: 16px 24px;
  }
`;
