import { ButtonWrapper } from '@/components/ui/Button/styles';
import styled from 'styled-components';

export const CreateProjectButton = styled.div`
  ${ButtonWrapper} {
    width: fit-content;
    white-space: nowrap;
  }
`;
