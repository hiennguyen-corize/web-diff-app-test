import styled from 'styled-components';

export const ErrorWrapper = styled.div`
  padding-left: 20px;
  color: ${({ theme }) => theme.colors.false_700};
`;

export const ListUrlItem = styled.ul`
  padding: 0 20px;
`;
